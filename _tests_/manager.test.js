const Manager = require('../lib/Manager')

test('test name, id, email, phone of manager', () => {
    const newManger = new Manager('Awa'
    const newManager = new Manager(name)
    expect(newManager.name).toBe(name)
})